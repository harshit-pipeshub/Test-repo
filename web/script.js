// Small script for the sample web page

document.addEventListener('DOMContentLoaded', function () {
  const el = document.getElementById('message');
  el.textContent = 'Hello from Test-repo sample web page!';
  console.log('Test-repo sample script loaded');
});
